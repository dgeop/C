#include <stdio.h>

void main()
{
    int a, b, c;
    a = 5;
    b = 16;

    c = a + b;
    printf("Soma entre A + B = " "%d", c);

    c = a - b;
    printf("\nSubtracao entre A + B = " "%d", c);

    c = a * b;
    printf("\nMultiplicacao entre A + B = " "%d", c);

    c = a / b;
    printf("\nDivisao entre A + B = " "%d", c);
}
